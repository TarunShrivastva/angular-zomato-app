import { ItemManagePipe } from './item-manage.pipe';

describe('ItemManagePipe', () => {
  it('create an instance', () => {
    const pipe = new ItemManagePipe();
    expect(pipe).toBeTruthy();
  });
});
